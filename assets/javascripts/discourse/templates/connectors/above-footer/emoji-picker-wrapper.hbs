{{#if this.renderRetortPicker}}
  {{emoji-picker
    forRetort=true
    isActive=isActive
    post=post
    emojiSelected=emojiSelected
    onEmojiPickerClose=(action (mut isActive) false)
  }}
{{/if}}